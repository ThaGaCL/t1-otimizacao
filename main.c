#include <stdio.h>
#include <stdlib.h>


int main(int argc, int *argv[]) {
    int cidades, rotas, recursos, pacotes, ganho;
    
    scanf("%d %d %d %d %d", &cidades, &rotas, &recursos, &pacotes, &ganho);
        
    return 0;
}